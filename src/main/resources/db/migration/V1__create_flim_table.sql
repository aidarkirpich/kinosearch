CREATE TABLE `testschema`.`f1_0` (
                                      `id` INT NOT NULL AUTO_INCREMENT,
                                      `filmId` INT NULL,
                                      `name` VARCHAR(250) NULL,
                                      `year` INT NULL,
                                      `rating` FLOAT NULL,
                                      `discription` VARCHAR(2500) NULL,
                                      PRIMARY KEY (`id`));